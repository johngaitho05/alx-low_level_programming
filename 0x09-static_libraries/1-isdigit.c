#include "main.h"

/**
 * main- the entry point
 * Return: 0
 */
int main()
{
	return (0);
}

/**
 * _isdigit - checks for a digit (0 through 9)
 * @c: int to be checked
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9' ? 1 : 0);
}

